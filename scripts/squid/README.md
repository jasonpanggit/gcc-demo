# Squid Script

This folder contains a Squid proxy bootstrap script for lab egress scenarios.

## File

- `squid-config.sh`

## Typical use

- Manual setup on a Linux VM used as a proxy jump point
- Optional compute path extension when proxy scenarios are enabled

Related modules:

- `modules/compute`
- `modules/firewall`
