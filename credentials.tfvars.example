# ==============================================================================
# AZURE CREDENTIALS TEMPLATE
# ==============================================================================
# Copy this file to 'credentials.tfvars' and update with your Azure credentials
# 
# SECURITY NOTICE:
# - Never commit credentials.tfvars to version control
# - The .gitignore file is configured to exclude credentials.tfvars
# - Use Azure Key Vault or environment variables for production deployments

# Azure Provider Authentication (REPLACE WITH YOUR VALUES)
subscription_id = "your-azure-subscription-id-here"       # Your Azure subscription ID
tenant_id       = "your-azure-tenant-id-here"             # Your Azure AD tenant ID  
client_id       = "your-service-principal-app-id-here"    # Service principal application ID
client_secret   = "your-service-principal-secret-here"    # Service principal secret

# ==============================================================================
# HOW TO GET THESE VALUES
# ==============================================================================
# 1. Subscription ID: Azure Portal > Subscriptions > [Your Subscription] > Overview
# 2. Tenant ID: Azure Portal > Azure Active Directory > Overview > Tenant ID
# 3. Service Principal: Create via Azure CLI or Portal
#    - Azure CLI: az ad sp create-for-rbac --name "terraform-sp" --role="Contributor"
#    - Portal: Azure AD > App registrations > New registration
# 4. Client Secret: Azure AD > App registrations > [Your App] > Certificates & secrets

# ==============================================================================
# ALTERNATIVE: ENVIRONMENT VARIABLES
# ==============================================================================
# Instead of this file, you can set environment variables:
# export TF_VAR_subscription_id="your-subscription-id"
# export TF_VAR_tenant_id="your-tenant-id"
# export TF_VAR_client_id="your-client-id"
# export TF_VAR_client_secret="your-client-secret"
