[pytest]
# Pytest configuration for UI tests

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Markers for test categorization
markers =
    smoke: Smoke tests (quick, critical path)
    regression: Regression tests (comprehensive)
    accessibility: Accessibility tests
    performance: Performance tests
    responsive: Responsive design tests
    navigation: Navigation tests
    slow: Slow running tests
    remote: Remote tests requiring live Azure endpoints
    phase1: Phase 1 regression gate
    phase2: Phase 2 regression gate
    phase3: Phase 3 regression gate
    phase4: Phase 4 regression gate

# Test timeout
timeout = 60

# Console output
console_output_style = progress

# Minimum Python version
minversion = 3.8
